<Window x:Class="VUWare.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VUWare.App"
        mc:Ignorable="d"
        Title="VU1 Dial Sensor Monitor"
        Height="161"
        Width="282"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Background="White">
    <Window.Resources>
        <Style x:Key="DialButtonStyle" TargetType="Button">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Percentage}" FontSize="18" FontWeight="Bold" TextAlignment="Center"/>
                            <TextBlock Text="{Binding DisplayName}" FontSize="9" TextAlignment="Center" Margin="0,2,0,0" Foreground="#666666" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid VerticalAlignment="Top">
        <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10">
            <StackPanel Orientation="Horizontal" Width="265">
                <Button x:Name="Dial1Button" Style="{StaticResource DialButtonStyle}" Width="55" Height="40" Margin="5" Background="#CCCCCC"/>
                <Button x:Name="Dial2Button" Style="{StaticResource DialButtonStyle}" Width="55" Height="40" Margin="5" Background="#CCCCCC"/>
                <Button x:Name="Dial3Button" Style="{StaticResource DialButtonStyle}" Width="55" Height="40" Margin="5" Background="#CCCCCC"/>
                <Button x:Name="Dial4Button" Style="{StaticResource DialButtonStyle}" Width="55" Height="40" Margin="5" Background="#CCCCCC"/>
            </StackPanel>
            <Button x:Name="StatusButton" Content="Initializing" Height="40" Margin="5" Width="250"/>
        </StackPanel>
    </Grid>
</Window>
