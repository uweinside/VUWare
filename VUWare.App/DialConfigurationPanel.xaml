<UserControl x:Class="VUWare.App.DialConfigurationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:VUWare.App.Converters"
             mc:Ignorable="d"
             Background="#2a2a2a"
             Foreground="#CCCCCC">
    <UserControl.Resources>
        <converters:ColorModeToVisibilityConverter x:Key="ColorModeToVisibilityConverter"/>
        <converters:UpperCaseConverter x:Key="UpperCaseConverter"/>
        
        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Padding="{TemplateBinding Padding}"
                                        VerticalScrollBarVisibility="Disabled"
                                        HorizontalScrollBarVisibility="Auto"
                                        Focusable="False"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="#3a3a3a"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#888888"/>
                                <Setter Property="Background" Value="#1a1a1a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        Focusable="False"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}"
                                              CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 0 0 L 4 4 L 8 0 Z"
                                                    Fill="#CCCCCC"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#333333"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="{TemplateBinding Padding}"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True"
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="200">
                                    <Border Background="#2a2a2a"
                                          BorderBrush="Transparent"
                                          BorderThickness="0"
                                          CornerRadius="6"
                                          Margin="0,4,0,0">
                                        <ScrollViewer Margin="4">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}"
                              Padding="{TemplateBinding Padding}"
                              CornerRadius="4"
                              Margin="2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#0078d4"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#0078d4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="#1a1a1a" BorderBrush="#444444" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
        <StackPanel>
            <!-- Dial Header with Enable Toggle -->
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding DialNumber, StringFormat='Dial #{0}'}" 
                           FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center" Grid.Column="0"/>
                <CheckBox x:Name="EnabledCheckBox" IsChecked="{Binding Enabled}" 
                         VerticalAlignment="Center" Grid.Column="1" Foreground="#CCCCCC" Content="Enabled"/>
            </Grid>

            <!-- Sensor Configuration Section -->
            <TextBlock Text="Sensor Configuration" FontSize="12" FontWeight="Bold" Foreground="#FFFF99" Margin="0,0,0,10"/>
            
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Display Name:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="0" Grid.Column="0"/>
                <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" 
                        Height="28" Grid.Row="0" Grid.Column="1" MinWidth="300"/>

                <TextBlock Text="Sensor Name:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="2" Grid.Column="0"/>
                <ComboBox ItemsSource="{Binding AvailableSensors}" 
                         SelectedItem="{Binding SensorName, UpdateSourceTrigger=PropertyChanged}" 
                         Height="28" Grid.Row="2" Grid.Column="1" 
                         MinWidth="400" IsTextSearchEnabled="True"/>

                <TextBlock Text="Entry Name:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="4" Grid.Column="0"/>
                <ComboBox ItemsSource="{Binding AvailableEntries}" 
                         SelectedItem="{Binding EntryName, UpdateSourceTrigger=PropertyChanged}"
                         Height="28" Grid.Row="4" Grid.Column="1" 
                         MinWidth="300" IsTextSearchEnabled="True"/>
            </Grid>

            <!-- Display Format Section -->
            <TextBlock Text="Display Settings" FontSize="12" FontWeight="Bold" Foreground="#FFFF99" Margin="0,10,0,10"/>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Display Format:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="0" Grid.Column="0"/>
                <ComboBox ItemsSource="{Binding DisplayFormats}" 
                         SelectedItem="{Binding DisplayFormat}" Height="28" Width="150" Grid.Row="0" Grid.Column="1" 
                         HorizontalAlignment="Left">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource UpperCaseConverter}}" Foreground="#FFFFFF"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
     
                <TextBlock Text="Display Unit:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="2" Grid.Column="0"/>
                <TextBox Text="{Binding DisplayUnit, UpdateSourceTrigger=PropertyChanged}" 
                        Height="28" Width="150" Grid.Row="2" Grid.Column="1"  HorizontalAlignment="Left"/>
            </Grid>

            <!-- Value Range Section -->
            <TextBlock Text="Value Range" FontSize="12" FontWeight="Bold" Foreground="#FFFF99" Margin="0,10,0,10"/>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Min Value:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="0" Grid.Column="0"/>
                <TextBox Text="{Binding MinValue, UpdateSourceTrigger=PropertyChanged}" 
                        Height="28" Grid.Row="0" Grid.Column="1" Width="150" HorizontalAlignment="Left"/>

                <TextBlock Text="Max Value:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="0" Grid.Column="3"/>
                <TextBox Text="{Binding MaxValue, UpdateSourceTrigger=PropertyChanged}" 
                        Height="28" Grid.Row="0" Grid.Column="4" Width="150" HorizontalAlignment="Left"/>

                <TextBlock Text="Warning Threshold:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="2" Grid.Column="0"/>
                <TextBox Text="{Binding WarningThreshold, UpdateSourceTrigger=PropertyChanged}" 
                        Height="28" Grid.Row="2" Grid.Column="1" Width="150" HorizontalAlignment="Left"/>

                <TextBlock Text="Critical Threshold:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="2" Grid.Column="3"/>
                <TextBox Text="{Binding CriticalThreshold, UpdateSourceTrigger=PropertyChanged}" 
                        Height="28" Grid.Row="2" Grid.Column="4" Width="150" HorizontalAlignment="Left"/>

                <TextBlock Text="Update Interval (ms):" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="4" Grid.Column="0"/>
                <TextBox Text="{Binding UpdateIntervalMs, UpdateSourceTrigger=PropertyChanged}" 
                        Height="28" Grid.Row="4" Grid.Column="1" Width="150" HorizontalAlignment="Left" TextChanged="TextBox_TextChanged"/>
            </Grid>

            <!-- Color Configuration Section -->
            <TextBlock Text="Color Configuration" FontSize="12" FontWeight="Bold" Foreground="#FFFF99" Margin="0,10,0,10"/>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Color Mode:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="0" Grid.Column="0"/>
                <ComboBox ItemsSource="{Binding ColorModes}" 
                         SelectedItem="{Binding ColorMode}" Height="28" Grid.Row="0" Grid.Column="1"
                         Width="150" HorizontalAlignment="Left">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource UpperCaseConverter}}" Foreground="#FFFFFF"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>

            <!-- Static Color (visible when ColorMode == "static") -->
            <Grid Margin="0,0,0,10" 
                  Visibility="{Binding ColorMode, Converter={StaticResource ColorModeToVisibilityConverter}, ConverterParameter=static}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="Static Color:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Column="0"/>
                <ComboBox ItemsSource="{Binding AvailableColors}" 
                         SelectedItem="{Binding StaticColor}" Height="28" Grid.Column="1"
                         Width="150" HorizontalAlignment="Left"/>
            </Grid>

            <!-- Threshold Colors (visible when ColorMode == "threshold") -->
            <Grid Visibility="{Binding ColorMode, Converter={StaticResource ColorModeToVisibilityConverter}, ConverterParameter=threshold}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Normal Color:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="0" Grid.Column="0"/>
                <ComboBox ItemsSource="{Binding AvailableColors}" 
                         SelectedItem="{Binding NormalColor}" Height="28" Grid.Row="0" Grid.Column="1"
                         Width="150" HorizontalAlignment="Left"/>

                <TextBlock Text="Warning Color:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="2" Grid.Column="0"/>
                <ComboBox ItemsSource="{Binding AvailableColors}" 
                         SelectedItem="{Binding WarningColor}" Height="28" Grid.Row="2" Grid.Column="1"
                         Width="150" HorizontalAlignment="Left"/>

                <TextBlock Text="Critical Color:" VerticalAlignment="Center" Foreground="#CCCCCC" Grid.Row="4" Grid.Column="0"/>
                <ComboBox ItemsSource="{Binding AvailableColors}" 
                         SelectedItem="{Binding CriticalColor}" Height="28" Grid.Row="4" Grid.Column="1"
                         Width="150" HorizontalAlignment="Left"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
